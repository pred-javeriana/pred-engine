"""Modelling layer.

Implements the BaseForecaster contract and concrete model wrappers
(Seasonal Naive, statistical, ML, and deep-learning families).  Every
model must accept a deterministic seed so that runs are reproducible.
"""
